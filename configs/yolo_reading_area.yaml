experiment:
  name: yolo_reading-area-v3-check
  run_name: yolo11s_img640-epochs50

mlflow:
  tracking_uri: ${MLFLOW_TRACKING_URI}

train:
  task: detect
  model: yolo11s.pt
  data: datasets/yolo_reading_area/data.yaml

  epochs: 50
  imgsz: 640
  batch: 16

  project: runs/yolo
  name: reading_area_yolo11s
